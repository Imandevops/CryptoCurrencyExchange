# TEMPLATE ONLY. Do not commit an applied Secret with real values.
# Create the real Secret from your CI secret store or a secret-management system.
apiVersion: v1
kind: Secret
metadata:
  name: cryptocurrency-exchange-secrets
  namespace: cryptocurrency-exchange
type: Opaque
stringData:
  # Generate with: openssl rand -base64 48
  DJANGO_SECRET_KEY: "REPLACE_WITH_A_RANDOM_DJANGO_SECRET"
  DATABASE_USER: "exchange_app"
  DATABASE_PASSWORD: "REPLACE_WITH_A_STRONG_DATABASE_PASSWORD"
  # PostgreSQL initialization uses these names as well.
  POSTGRES_USER: "exchange_app"
  POSTGRES_PASSWORD: "REPLACE_WITH_A_STRONG_DATABASE_PASSWORD"
